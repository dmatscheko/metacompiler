# tests/bench.sh baselines: instructions retired, MEDIAN OVER 5 LAYOUT DRAWS,
# native -exe binaries built from tests/bench/mod.*. Re-record with
# tests/bench.sh --record when you land an intended change, and say in the commit
# message which rows moved and why.
#
# recorded at commit 4b594c1, in a clean `git archive` tree and NOT in a working
# tree - the manual's SS4 trap applies to benchmarks too.
#
# WHY A MEDIAN OVER DRAWS AND NOT A NUMBER. A binary's instruction count depends
# on its CODE LAYOUT, and layout is perturbed by things that change no semantics.
# One unchanged kotlin source, varying only the length of the -exe output path:
#
#   17.353 17.494 17.620 17.911 17.915 17.916 17.916 18.018 18.023 18.182 G
#
# 4.8%, bimodal, from a file NAME. A module edit draws from the same distribution,
# so no pairing cancels it and a single A/B build cannot resolve a sub-2% effect.
# Three separate attempts to measure one change this way returned +0.04%, +0.65%
# and -1.72%.
#
# EACH ROW'S OWN SPREAD, at 5 draws, measured with these baselines - this is the
# floor below which that row can say nothing:
#
#   c 0.35%   kotlin 1.05%   python 1.31%   lua 2.04%   java 3.21%   ruby 4.14%
#
# The gate therefore ignores any delta smaller than the row's own spread, however
# large it looks: java has been observed at +3.20% against a 3.21% spread, which
# is a coin landing heads. `c` is both the semantic control (self-contained IR,
# no js_* extern) and the tightest row, which makes it the one to read first.
c          11094945
java       8944228510
kotlin     18018909347
lua        1499593760
python     31086559787
ruby       6224352876
