# tests/bench.sh baselines: instructions retired, min of 3 runs,
# native -exe binaries built from tests/bench/mod.*. Re-record with
# tests/bench.sh --record when you land an intended change, and say
# in the commit message which rows moved and why.
# recorded at commit e1307f3, in a clean `git archive e1307f3` tree and NOT in a
# working tree - the manual's SS4 trap applies to benchmarks too, and the working
# tree at the time (three agents mid-edit) read 0.4-0.8% off these on three rows.
#
# REPRODUCIBILITY, measured rather than assumed: re-running the whole harness
# against these numbers moves each row by at most +-0.02% (ruby, the row that
# looked noisiest, read -0.02 / -0.01 / +0.01 on three consecutive runs). The
# default 1.0% tolerance is therefore ~50x the noise floor; tighten it with
# --tol if you are chasing something small.
#
# For reference, the kotlin row IS the harness docs/runtime-merge-plan.md uses,
# and 17.912 G here is the same measurement as the 18.023 G quoted there for the
# post-hash-index build (that one was taken in a working tree).
c          11272781
java       8664605619
kotlin     17912240238
lua        1499717068
python     31704073506
ruby       6116559760
